import styled from '@emotion/styled'
import { OnPrimary90 } from '../../styles/global'

export const Wrapper = styled.div`
  margin-bottom: 1.5rem;

  padding: 0 1rem;

  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 8px;

  border: 1px solid ${OnPrimary90};

  border-radius: 0.4rem;
`

export const Text = styled.h4`
  font-size: 1.6rem;
`
